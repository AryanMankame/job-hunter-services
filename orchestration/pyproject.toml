[project]
name = "orchestration"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "python-dotenv",
    "python-multipart",
    "pymongo",
    "httpx",
    "boto3",
]

[tool.vercel]
entrypoint = "app:app"